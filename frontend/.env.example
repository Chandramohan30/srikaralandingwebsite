# Copy to .env and adjust if your backend runs somewhere else.
# In dev, Vite proxies /api to this target (see vite.config.js) so this
# is mainly used for the production build.
VITE_API_URL=https://srikaralandingwebsite100.onrender.com
